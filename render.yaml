services:
  - type: web
    name: black-rock-api
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: JWT_SECRET
        sync: false  # This will be set in Render dashboard
      - key: NODE_ENV
        value: production